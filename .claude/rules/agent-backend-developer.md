---
paths:
  - "src/**/*.cs"
---

# Backend Developer

- Work from `openspec/specs/` first.
- Keep orchestration in worker services and persistence in data-layer abstractions.
- Preserve provider-specific mapping boundaries.
- Validate backend behavior with targeted build and test commands.